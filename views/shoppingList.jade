extends templates/layout

block content
    .container
        .panel.panel-default
            .panel-heading Shopping List
            table(class="table table-striped")
                tr.table-header
                    td.name-column Name
                    td.amount-column Amount
                    - for (var i = 0; i < shoppingData.length; i++) {
                        - var currentItem = shoppingData[i];
                        - if (currentItem.isClaimed === true) {
                            tr(class="shopping-list-item strikethrough", id=i)
                                td.item-name-column
                                    = currentItem.name
                                td.amount-column
                                    = currentItem.amount
                        - } else {
                            tr(class="shopping-list-item", id=i)
                                td.item-name-column
                                    = currentItem.name
                                td.amount-column
                                    = currentItem.amount                      
                        - }
                    - }
    .container
        .panel.panel-primary
            .panel-heading Add Item
            .panel-body 
                form(class="form-inline" action="/shoppingList/Create", method="post")
                    .form-group
                        label(for='name')
                            | Name
                        input.form-control(type='text', id='name', name='name', required)
                    .form-group
                        label(for='amount')
                            | Amount
                        input.form-control(type='number', id='amount', name='amount', required)
                    .form-group
                        button.btn.btn-primary(type='submit') Submit

